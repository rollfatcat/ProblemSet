// Greedy
#include<bits/stdc++.h>
using namespace std;
#define MAXN 10001

bool hasT[MAXN];
struct PAIR{ short cost, ; } ;
int main(){
  int N, M;
  while(scanf("%d",&N) and N){
    int nonT=0;
    for(int x,i=1;i<=N;i++)
      scanf("%d",&x),
      hasT[i]=(x!=0);
    scanf("%d",&M);
    //
    int tot=0;

    for(int i=2;i<=N;i++)

    //
    while(M){

    }
  }
}