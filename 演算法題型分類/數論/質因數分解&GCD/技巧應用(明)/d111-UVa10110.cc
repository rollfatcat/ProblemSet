// 因數個數是奇數時輸出"no"偶數輸出"yes" => 只有平方數的因數是奇數
#include<iostream>
#include<cmath>
using namespace std;

int main(){
  for(int x, root; cin>>x and x; )
    root=sqrt(x),
    cout<<((root*root==x)?"yes":"no")<<endl;
}